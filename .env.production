# GA4 Measurement ID — property "arvanitisconstruction.gr" (Laliotis).
# Committed on purpose: measurement IDs are public (they ship in the JS bundle).
# Hostinger builds from the GitHub clone, which has no local .env, so this file
# is what enables analytics + the cookie banner on production builds.
VITE_GA_ID=G-9VJ9DKPC0X
